/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatar-bucket-elec5620.s3.ap-southeast-2.amazonaws.com'], // 替换为你的 S3 域名
    },
  };
  

  


export default nextConfig;
